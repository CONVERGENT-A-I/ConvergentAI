name: Deploy CONVERGENT-AI to GCP VM

on:
  push:
    branches:
      - dev
  workflow_dispatch:
    inputs:
      target:
        description: "Which environment to deploy"
        required: true
        default: production
        type: choice
        options:
          - production
          - dev

concurrency:
  group: deploy-${{ github.ref_name }}-${{ github.event.inputs.target || 'auto' }}
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Resolve target
        id: env
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TARGET="${{ github.event.inputs.target }}"
          else
            TARGET="dev"
          fi
          if [ "$TARGET" = "production" ]; then
            echo "branch=main"                                 >> "$GITHUB_OUTPUT"
            echo "git_dir=/opt/convergent-ai/ConvergentAI"     >> "$GITHUB_OUTPUT"
            echo "compose_dir=/opt/convergent-ai/ConvergentAI" >> "$GITHUB_OUTPUT"
            echo "env_name=production"                         >> "$GITHUB_OUTPUT"
          else
            echo "branch=dev"                                                     >> "$GITHUB_OUTPUT"
            echo "git_dir=/opt/convergent-ai/convergent-dev/ConvergentAI"         >> "$GITHUB_OUTPUT"
            echo "compose_dir=/opt/convergent-ai/convergent-dev/docker"           >> "$GITHUB_OUTPUT"
            echo "env_name=dev"                                                   >> "$GITHUB_OUTPUT"
          fi

      - name: Auth to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      # Build the remote script LOCALLY with values already substituted, then
      # pipe it to the VM. No env passing through sudo (which is what broke it).
      - name: Render remote deploy script
        id: script
        env:
          BRANCH: ${{ steps.env.outputs.branch }}
          GIT_DIR: ${{ steps.env.outputs.git_dir }}
          COMPOSE_DIR: ${{ steps.env.outputs.compose_dir }}
        run: |
          set -euo pipefail
          cat > remote_deploy.sh <<REMOTE
          #!/usr/bin/env bash
          set -euo pipefail
          BRANCH="${BRANCH}"
          GIT_DIR="${GIT_DIR}"
          COMPOSE_DIR="${COMPOSE_DIR}"

          echo "==> Target branch : \${BRANCH}"
          echo "==> Git directory : \${GIT_DIR}"
          echo "==> Compose dir   : \${COMPOSE_DIR}"

          # ---- 1) Update code ----
          git config --global --add safe.directory "\${GIT_DIR}" || true
          if ! git -C "\${GIT_DIR}" rev-parse --is-inside-work-tree >/dev/null 2>&1; then
            echo "ERROR: \${GIT_DIR} is not a git repo" >&2
            ls -la "\${GIT_DIR}" >&2 || true
            exit 1
          fi
          echo "==> Pull latest code (\${BRANCH})"
          git -C "\${GIT_DIR}" fetch origin "\${BRANCH}"
          git -C "\${GIT_DIR}" checkout "\${BRANCH}"
          git -C "\${GIT_DIR}" reset --hard "origin/\${BRANCH}"

          # ---- 2) Locate compose file ----
          cd "\${COMPOSE_DIR}"
          if   [ -f docker-compose.yaml ]; then COMPOSE="-f docker-compose.yaml"
          elif [ -f docker-compose.yml  ]; then COMPOSE="-f docker-compose.yml"
          elif [ -f docker/docker-compose.yaml ]; then COMPOSE="-f docker/docker-compose.yaml"
          else
            echo "No docker-compose file under \${COMPOSE_DIR}" >&2
            ls -la "\${COMPOSE_DIR}" >&2
            exit 1
          fi
          echo "==> Compose dir: \$(pwd)  file: \${COMPOSE}"

          dc() { docker compose \${COMPOSE} "\$@" || docker-compose \${COMPOSE} "\$@"; }

          echo "==> Docker usage BEFORE"
          docker system df || true
          echo "==> Stop old containers"
          dc down || true
          echo "==> Rebuild + recreate"
          dc up -d --build
          echo "==> Verify"
          docker ps | grep convergent || true
          echo "==> Cleanup"
          docker builder prune -a -f || true
          docker image prune -f || true
          docker container prune -f || true
          REMOTE
          echo "----- rendered remote_deploy.sh -----"
          cat remote_deploy.sh

      - name: Deploy to GCP VM (${{ steps.env.outputs.env_name }})
        run: |
          set -euo pipefail
          # Pipe the fully-rendered script to a root shell on the VM via stdin.
          gcloud compute ssh convergent-ai \
            --zone us-east1-b \
            --project ailana-488814 \
            --tunnel-through-iap \
            --quiet \
            --command="sudo bash -s" < remote_deploy.sh
